# 企业知识库 - 极简依赖（无 PyTorch，节省资源）
fastembed>=0.3.0          # 本地 Embedding（量化 ONNX，内存占用小）
chromadb>=0.5.0           # 向量库（本地文件，无独立进程）
openai>=1.30.0            # 调用 yxkl 中转 Chat（OpenAI 兼容）
python-dotenv>=1.0.0      # 读取 .env 密钥
python-docx>=1.1.0        # 解析 Word
pypdf>=4.0.0              # 解析 PDF
openpyxl>=3.1.0           # 解析 Excel
fastapi>=0.110.0          # Web 后端（第二期）
uvicorn>=0.29.0           # ASGI 服务器（第二期）
python-multipart>=0.0.9   # 解析上传的文件（multipart/form-data）
