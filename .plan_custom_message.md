# 自定义消息编辑功能实现计划

## 需求
1. 修改访问地址：去掉末尾斜杠 ✅ 已完成
2. 合并"发送规则介绍"到"手动发送通报"中作为一个选项
3. 添加自定义消息编辑功能，支持多种消息类型：
   - text（文本）- 支持 @
   - markdown - 不支持 @，需要额外发一条 text @人
   - image（图片）- 不支持 @，需要额外发一条 text @人
   - news（图文）- 不支持 @，需要额外发一条 text @人

## 实现方案

### 1. 前端 UI 改造（index.html）
- 在"通报规则"下拉框中添加特殊选项：
  - "📋 发送规则介绍"
  - "✏️ 自定义消息"
- 当选择"自定义消息"时：
  - 隐藏"通报日期"字段
  - 显示"消息类型"选择器（text/markdown/image/news）
  - 根据消息类型显示对应的编辑界面
  - text/markdown: 文本编辑框
  - image: 图片 URL 或 base64 输入框
  - news: 图文卡片编辑（标题、描述、图片URL、跳转URL）
- 删除原有的独立"规则介绍发送卡片"

### 2. 前端状态管理（Alpine.js）
添加新的状态字段：
```javascript
snd: {
  mode: 'rule', // 'rule' | 'rules-intro' | 'custom'
  customType: 'text', // 'text' | 'markdown' | 'image' | 'news'
  customContent: {
    text: '',
    markdown: '',
    image: {url: '', base64: ''},
    news: {title: '', description: '', picurl: '', url: ''}
  }
}
```

### 3. 后端 API 扩展（api.py）
添加新端点：
- POST `/api/report/send-custom` - 发送自定义消息
  - 参数：message_type, content, recipient_names
  - 逻辑：
    - 如果是 text，直接发送
    - 如果是 markdown/image/news，先发送主消息，再发送 text @人

### 4. 后端发送器扩展（sender.py）
添加新函数：
- `send_markdown(content, recipients)` - 发送 Markdown
- `send_image(image_data, recipients)` - 发送图片
- `send_news(articles, recipients)` - 发送图文

## 文件清单
- wecom_notice/frontend/index.html - UI 改造
- wecom_notice/frontend/js/api.js - 添加 sendCustom 方法
- wecom_notice/api.py - 添加 /api/report/send-custom 端点
- wecom_notice/sender.py - 添加新消息类型发送函数
