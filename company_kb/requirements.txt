# 企业知识库依赖（无 PyTorch，节省资源）
# fastapi/uvicorn/python-multipart/openpyxl 已移至根目录 requirements.txt
# 安装：pip install -r requirements.txt && pip install -r company_kb/requirements.txt

fastembed>=0.3.0       # 本地 Embedding（量化 ONNX，内存占用小）
chromadb>=0.5.0        # 向量库（本地文件，无独立进程）
openai>=1.30.0         # 调用中转 Chat（OpenAI 兼容接口）
python-dotenv>=1.0.0   # 读取 .env 密钥
python-docx>=1.1.0     # 解析 Word
pypdf>=4.0.0           # 解析 PDF
